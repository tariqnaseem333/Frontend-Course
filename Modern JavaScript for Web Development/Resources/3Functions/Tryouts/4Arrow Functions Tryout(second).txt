//One parameter, single line code
let trip = place => "Trip to " + place;
console.log(trip("Paris")); // Trip to Paris




//One parameter, single line code:
trip = _ => "Trip to " + _;
console.log(trip("Paris"));  // Trip to Paris
